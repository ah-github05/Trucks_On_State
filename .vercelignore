# Development files
node_modules
.git
.gitignore
.replit
*.md
replit.md

# Build files (will be rebuilt by Vercel)
dist

# Source photo assets not needed for deployment (already built into
# client/public by scripts/crop-image.py etc.) — scoped to attached_assets
# only so favicon/OG images checked into client/public are still deployed.
attached_assets/*.jpeg
attached_assets/*.png

# IDE and config files
.claude
